import { RequirementListDatePipe } from './requirement-list-date.pipe';

describe('RequirementListDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RequirementListDatePipe();
    expect(pipe).toBeTruthy();
  });
});
