import { CourseRolesPipe } from './course-roles.pipe';

describe('CourseRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
