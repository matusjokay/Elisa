import { CourseRolesAvailablePipe } from './course-roles-available.pipe';

describe('CourseRolesAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseRolesAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
